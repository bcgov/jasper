using System;
using Scv.Api.Controllers;
using Scv.Api.Services;
using Scv.Api.Models;

namespace Scv.Api.Mappers
{
    public class Mappings // : AutoMapper.Profile
    {
        public Mappings()
        {
            // CreateMap<Assignment, AssignmentDto>();
            // CreateMap<AssignmentDto, Assignment>();

           // CreateMap<AssignmentService.JudicialCalendarAssignment, Models.Assignment.Assignment>()
          //      .ForMember(d => d.Id, opts => opts.Ignore())
           //     .ForMember(d => d.UserId, opts => opts.MapFrom(s => s.Id))
           //     .ForMember(d => d.LocationName, opts => opts.MapFrom(s => s.Location))
           //     .ForMember(d => d.Title, opts => opts.MapFrom(s => s.Title))
           //     .ForMember(d => d.Start, opts => opts.MapFrom(s => s.Start))
           //     .ReverseMap();
        }


    }
 
}





 